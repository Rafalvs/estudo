<?php

/*
EX1
Faça um algoritmo que determine o maior entre N números. A condição de parada é a entrada de um valor 0, ou seja,
o algoritmo deve ficar calculando o maior até que a entrada seja igual a 0 (ZERO).

versao com python

n = 1 # numero que executara o loop e recebera a entrada do usuario
x = 0 # x sera usado para comparar

while(n != 0): # enquanto 0 nao for digitado o loop segue

    n = int(input("Digite um número: ")) # pede a entrada de um numero

    if(n > x):
        x = 0 # mantem o x sempre vazio
        x = n1 + x # x vai guardar o maior valor sempre

print(x) # printa o maior numero

EX2
Faça um algoritmo que conte de 1 a 100 e a cada múltiplo de 10 emita uma mensagem: “Múltiplo de 10”.

$x = 0;

while($x < 101)
{
    if($x % 10 == 0)
    {
        echo("Multiplo de 10: " . $x);
    }
    $x++;
}

EX3
Uma rainha requisitou os serviços de um monge e disse-lhe que pagaria qualquer preço. O monge, necessitando de alimentos,
indagou à rainha sobre o pagamento, se poderia ser feito com grãos de trigo dispostos em um tabuleiro de xadrez,
de tal forma que o primeiro quadro deveria conter apenas um grão e os quadros subsequentes, o dobro do quadro anterior.
A rainha achou o trabalho barato e pediu que o serviço fosse executado, sem se dar conta de que seria impossível efetuar
o pagamento. Faça um algoritmo para calcular o número de grãos que o monge esperava receber.

$x = 1;
$y = 0;

while($x < 1000)
{
    $x = $x + $x
    echo($x);
}

*/

?>